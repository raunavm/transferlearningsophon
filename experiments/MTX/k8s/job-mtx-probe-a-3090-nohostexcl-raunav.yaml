apiVersion: batch/v1
kind: Job
metadata:
  # CAPACITY PROBE a-3090-nohostexcl -- 3090 + us-west, WITHOUT the ry-gpu-03 exclusion I added. Tests whether that exclusion is costing capacity.
  # Real resource shape (1 GPU, 76Gi, 4 CPU) so a schedule here means a REAL
  # training pod would also schedule. Sleeps 60 s and exits. Trains nothing.
  name: mtx-probe-a-3090-nohostexcl-raunav
  namespace: cms-ml
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 1800
  template:
    spec:
      restartPolicy: Never
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: topology.kubernetes.io/region
                operator: In
                values: ["us-west"]
              - key: nvidia.com/gpu.product
                operator: In
                values: ["NVIDIA-GeForce-RTX-3090"]

      containers:
      - name: probe
        image: gitlab-registry.nrp-nautilus.io/escheuller/transfer-learning:cu121
        command: ["/bin/bash", "-c"]
        args:
        - |
          echo "PROBE a-3090-nohostexcl scheduled on ${NODE_NAME}"
          nvidia-smi --query-gpu=name,memory.total --format=csv,noheader || true
          sleep 60
        env:
        - name: NODE_NAME
          valueFrom: { fieldRef: { fieldPath: spec.nodeName } }
        resources:
          requests: { memory: "76Gi", cpu: "4", nvidia.com/gpu: "1" }
          limits:   { memory: "76Gi", cpu: "4", nvidia.com/gpu: "1" }
